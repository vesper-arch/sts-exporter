return {
    {% for potion in potions
    %}["{{ potion.name }}"] = {
        Image = "{{ potion.image.relativeTo(dir) }}",
        Character = "",
        Rarity = "{{ potion.rarity }}",

    },
    {% endfor %}
}
